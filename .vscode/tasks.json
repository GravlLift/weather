{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Neu Build",
      "command": "neu build -v",
      "group": {
        "isDefault": true,
        "kind": "build"
      }
    },
    // Maybe one day...
    {
      "type": "shell",
      "label": "EsBuild",
      "command": "npx esbuild ./src/app.ts --bundle --outfile=app/assets/app.js --target=es6",
      "group": "build"
    }
  ]
}
